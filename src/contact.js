import './styles.css';

function contactComponent() {
    const content = document.createElement("h1");
    content.textContent = "This is for the contact info";

    return content;
}

export default contactComponent;